spnr.dom.id()
spnr.dom.id(id)
var elem = spnr.dom.id('anElementId')
A shorter way to call document.getElementById(). Returns the element with the id ```id```
/src/dom.js
nolink

NEXTSECTION

spnr.dom.viewportWidth()
spnr.dom.viewportWidth()
var width = spnr.dom.viewportWidth();
Get the width of the viewport.
/src/dom.js
nolink

NEXTSECTION

spnr.dom.viewportHeight()
spnr.dom.viewportHeight()
var height = spnr.dom.viewportHeight();
Get the height of the viewport.
/src/dom.js
nolink

NEXTSECTION

spnr.dom.viewportSize()
spnr.dom.viewportSize()
var size = spnr.dom.viewportSize();
Get the size of the viewport in a spnr vector.
/src/dom.js
nolink

NEXTSECTION

spnr.dom.logPara
spnr.dom.logPara
var elem = spnr.dom.logPara
A paragraph element which can be used to log data to. Only created when spnr.dom.logToPara is called. Has access methods spnr.dom.clearLogPara and spnr.dom.logToPara
/src/dom.js
nolink

NEXTSECTION

spnr.dom.clearLogPara()
spnr.dom.clearLogPara();
spnr.dom.clearLogPara();
If spnr.dom.logPara is defined, clear it
/src/dom.js
nolink

NEXTSECTION

spnr.dom.logToPara()
spnr.dom.logToPara(data, label='No label');
spnr.dom.logToPara(testResult, 'Test Result')
Append to spnr.dom.logPara ```data``` and ```label```, formatted in this way: ```label : data```
/src/dom.js
nolink

NEXTSECTION

spnr.dom.delete()
spnr.dom.delete(id)
spnr.dom.delete()
Delete (fully remove) the HTML element with the id ```id```. If no element with the id is found, then nothing will happen.
/src/dom.js
nolink