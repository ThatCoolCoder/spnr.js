spnr.round()
spnr.round(num, decimalPlaces=0)
var result = spnr.round(1.15, 1)
Round ```num``` so that the amount of decimal places is equal to ```decimalPlaces```. In this example the result would be 1.2.
/src/math.js
nolink

NEXTSECTION

spnr.floor()
spnr.floor(num, decimalPlaces=0)
var result = spnr.floor(1.15, 1)
Floor ```num``` so that the amount of decimal places is equal to ```decimalPlaces```. In this example the result would be 1.1.
/src/math.js
nolink

NEXTSECTION

spnr.ceiling()
spnr.ceiling(num, decimalPlaces=0)
var result = spnr.ceiling(1.15, 1)
Ceiling ```num``` so that the amount of decimal places is equal to ```decimalPlaces```. In this example the result would be 1.2.
/src/math.js
nolink

NEXTSECTION

spnr.randflt()
spnr.randflt(min, max)
var result = spnr.randflt(0, 1)
Returns a random float in the half-open range ```[min, max)```. This includes ```min``` but not ```max```. Possible results in the example would range from 0.0 to 0.999...
/src/math.js
nolink

NEXTSECTION

spnr.randint()
spnr.randint(min, max)
var result = spnr.randflt(0, 4)
Returns a random integer in the half-open range ```[min, max)```. This includes ```min``` but not ```max```. Possible results from the example would be [0, 1, 2, 3].
/src/math.js
nolink

NEXTSECTION

spnr.sigmoid()
spnr.sigmoid(x)
EXAMPLE GOES HERE
DESCRIPTION GOES HERE. FIND OUT SIGMOID DESCRIPTION.
/src/math.js
nolink

NEXTSECTION

spnr.invSigmoid()
spnr.invSigmoid(x)
EXAMPLE GOES HERE
DESCRIPTION GOES HERE. FIND OUT INVERSE SIGMOID DESCRIPTION.
/src/math.js
nolink

NEXTSECTION

spnr.degrees()
spnr.degrees(radians)
var degrees = spnr.degrees(spnr.PI / 2)
Convert an angle in radians (```radians```) into an angle in degrees. The example given would return 90.
/src/math.js
nolink

NEXTSECTION

spnr.radians()
spnr.radians(degrees)
var radians = spnr.radians(90)
Convert an angle in degrees (```degrees```) into an angle in radians. The example given would return 1.5707963267948966, or PI / 2.
/src/math.js
nolink

NEXTSECTION

spnr.mean()
spnr.mean(a, b)
var mean = spnr.mean(4, 6)
Return the arithmetic mean of ```a``` and ```b```. The example given would return 5. May be moved to another module at some point.
/src/math.js
nolink

NEXTSECTION

spnr._180DIVPI
spnr._180DIVPI
var radians = degrees / spnr._180DIVPI
The value of 180 divided by ```spnr.PI``` (see spnr Math wrappers). Allows for efficient conversion from degrees to radians and vice versa.
/src/math.js
nolink

NEXTSECTION

spnr Math wrappers
spnr Math wrappers
var a = spnr.PI
There is a spnr wrapper around every js Math function/value, EXCEPT for the values that are defined by spnr. An example of a Math method overwritten by spnr is ```spnr.round```, which is not identical to the Math variant.
/src/core.js
nolink