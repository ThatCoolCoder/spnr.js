spnr.arr.removeItem()
spnr.arr.removeItem(array, item)
spnr.arr.removeItem(users, userToDelete)
Try to remove ```item``` from ```array```. If ```array``` doesn't contain ```item```, creates a warning but doesn't break the program.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.removeIndex()
spnr.arr.removeIndex(array, index)
spnr.arr.removeIndex(users, userIndex)
Try to remove the item at ```index``` from ```array```. If ```index``` is out of bounds, creates a warning but doesn't break the program.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.highestIndex()
spnr.arr.highestIndex(array=[])
var highestIndex = spnr.arr.highestIndex([1, 4, 2, 3])
Return the index of the highest item in ```array```. In the example given, the output would be 1. If there are multiple lowest items, return the index of the last one. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.lowestIndex()
spnr.arr.lowestIndex(array=[])
var lowestIndex = spnr.arr.lowestIndex([4, 1, 4, 2])
Return the index of the lowest item in ```array```. In the example given, the output would be 1. If there are multiple lowest items, return the index of the last one. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.choose()
spnr.arr.choose(array=[])
var item = spnr.arr.choose(itemList)
Return a random item from ```array```. If ```array``` is empty or undefined, returns undefined;
/src/arr.js
nolink

NEXTSECTION

spnr.arr.sum()
spnr.arr.sum(array=[])
var total = spnr.arr.sum(scores)
Return the sum of all of the items in ```array```. If ```array``` is empty or undefined, returns 0. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.product()
spnr.arr.product(array=[])
var scoreMultiplier = spnr.arr.product(multipliers)
Return the product of all of the items in ```array```. If ```array``` is empty or undefined, returns 1. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.mean()
spnr.arr.mean(array=[])
var avgScore = spnr.arr.mean(scores)
Returns the mean (average) of all of the items in ```array```. If ```array``` is empty or undefined, returns NaN. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.median()
spnr.arr.median(array=[])
var avgScore = spnr.arr.median(scores)
Returns the median (middle value) of all of the items in ```array```. If ```array``` is empty or undefined, returns NaN. Recommended only for numbers.
/src/arr.js
nolink

NEXTSECTION

spnr.arr.mode()
spnr.arr.mode(array=[])
var mostCommonScores = spnr.arr.mode(scores)
Return a list containing the equal most common items in ```array```. If ```array``` is empty, returns an empty array. Works for all types, uses strict equality checking (```===```).
/src/arr.js
nolink